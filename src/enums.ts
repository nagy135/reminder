export enum Periodicity {
  daily = "DAILY",
  weekly = "WEEKLY",
  monthly = "MONTHLY",
  yearly = "YEARLY",
  oneTime = "ONETIME",
  custom = "CUSTOM",
}

export enum Page {
  home = "home",
  profile = "profile",
  calendar = "calendar",
}
